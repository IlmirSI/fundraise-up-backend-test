{
  "name": "fundraiseup",
  "version": "1.0.0",
  "description": "",
  "main": "sandbox.ts",
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\"",
    "start": "tsc && node dist/sandbox.js",
    "start:app": "tsc && node dist/app.js",
    "start:sync": "tsc && node dist/sync.js",
    "start:sync:reindex": "tsc && node dist/sync.js --full-reindex"
  },
  "author": "Ilmir Sadykov",
  "license": "ISC",
  "dependencies": {
    "@faker-js/faker": "^8.0.2",
    "mongodb": "^5.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.4.0",
    "prettier": "3.0.0",
    "typescript": "^5.1.6"
  }
}
